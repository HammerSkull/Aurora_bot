services:
  - type: web
    name: aurora-v7-final
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    buildpacks:
      - nodejs
      - python
