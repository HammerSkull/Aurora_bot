services:
  - type: web
    name: Aurora_bot
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    buildpacks:
      - nodejs
      - python
